// Schema definitions land in Tasks 10–12. This file re-exports from layer modules
// once they exist; for now it's empty so `db/client.ts`'s `import * as schema`
// resolves cleanly during typecheck.
export {}
